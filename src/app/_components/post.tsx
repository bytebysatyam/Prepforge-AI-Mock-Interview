"use client";

export function LatestPost() {
  return (
    <div className="w-full max-w-xs rounded-2xl border border-white/10 bg-white/5 p-4 text-sm text-slate-300">
      Demo post widget removed because this project no longer uses the default
      T3 `Post` model.
    </div>
  );
}
